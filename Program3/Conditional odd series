def generate_conditional_odd_series(a):
    series = [2 * i - 1 for i in range(1, (a // 2) + 2)]
    if a % 2 == 0:
        return series[:-1]
    return series

print(generate_conditional_odd_series(5)) 
print(generate_conditional_odd_series(9))  
